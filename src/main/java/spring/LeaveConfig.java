package spring;

import java.util.ArrayList;
import java.util.Date;
import java.util.List;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class LeaveConfig {

//    @Bean(name = "leaveList")
//    public LeaveList leaves() {
//        List<Leave> leaves = new ArrayList<>();   
//        System.out.println("UserList Constructed.");
//        leaves.add(new Leave());
//        System.out.println(leaves.get(0).getId() + " in UserList.");
//        leaves.add(new Leave(new Date(), new Date(), "Standard"));
//        System.out.println(leaves.get(1).getId() + " in UserList.");
////        for(int i=0; i<users.size(); i++){
////        System.out.println(users.get(i).getId());
////        }
//        return new LeaveList(leaves);
//    }
}
